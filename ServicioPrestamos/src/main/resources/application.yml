spring:
  application:
    name: ServicioPrestamos

  datasource:
    url: jdbc:postgresql://localhost:5441/db_prestamos
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8086
